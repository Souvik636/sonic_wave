/// Central single source of truth for application version.
class AppVersion {
  AppVersion._();

  /// Current application version (matches release tag e.g. "1.2.13").
  static const String current = '1.2.13';
}
