/// Central single source of truth for application version.
class AppVersion {
  AppVersion._();

  /// Current application version (matches release tag e.g. "1.2.6").
  static const String current = '1.2.6';
}
